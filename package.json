{
  "name": "baymax",
  "devDependencies": {
    "@baymax/core": "workspace:*",
    "typescript": "^5.8.0",
    "bun-types": "^1.3.9",
    "zod": "^3.25.0"
  },
  "private": true,
  "scripts": {
    "dev": "bun --cwd apps/server dev",
    "db:generate": "bun --cwd packages/core db:generate",
    "seed": "bun scripts/seed.ts",
    "import": "bun scripts/import-logs.ts",
    "tj": "bun scripts/tj.ts",
    "health": "bun apps/cli/src/index.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "workspaces": ["packages/*", "apps/*"]
}
